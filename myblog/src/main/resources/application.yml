spring:
  application:
    name: myblog
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true
  jpa:
    defer-datasource-initialization: true
    show-sql: true
jwt:
  issuer: guswhd903@navre.com
  secret_key: springboot-study